# Actual /docker/fit/.env is root-owned mode 0600; never print compose config.
DATABASE_URL=postgresql+asyncpg://fit:REPLACE_WITH_GENERATED_PASSWORD@postgres:5432/fit
TRUSTED_ORIGIN=https://fit.birek.online
