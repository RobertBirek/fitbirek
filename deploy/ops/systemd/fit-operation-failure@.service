[Unit]
Description=Report failed FitBirek operation %i

[Service]
Type=oneshot
ExecStart=/usr/bin/logger -p daemon.err -t fit-operations "FitBirek operation %i failed; inspect systemctl status and journalctl -u %i"
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
